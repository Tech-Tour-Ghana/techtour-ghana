docs/

node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.vercel
supabase/.temp/
supabase/.branches/
.playwright-mcp/

# Environment files. The negation must stay last, git lets the final
# matching pattern decide, so an earlier negation would be overridden.
.env
.env.*
!.env.example
